<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.petstore.api</groupId>
    <artifactId>petstore-karate-tests</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Petstore API Karate Test Automation Project</name>
    <description>
        Production-ready API test automation project for Petstore Swagger API using Karate framework.
        Aligned with ISO/IEC/IEEE 29119 Software Testing Standards.
        Risk-based testing approach with comprehensive coverage.
    </description>

    <properties>
        <!-- Project Encoding -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        
        <!-- Java Version -->
        <java.version>11</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        
        <!-- Dependency Versions -->
        <karate.version>1.4.1</karate.version>
        <junit5.version>5.10.1</junit5.version>
        <cucumber.version>7.14.0</cucumber.version>
        <gherkin.version>26.2.0</gherkin.version>
        <slf4j.version>2.0.9</slf4j.version>
        <logback.version>1.4.11</logback.version>
        
        <!-- Plugin Versions -->
        <maven.compiler.plugin.version>3.11.0</maven.compiler.plugin.version>
        <maven.surefire.plugin.version>3.0.0-M9</maven.surefire.plugin.version>
        <maven.failsafe.plugin.version>3.0.0-M9</maven.failsafe.plugin.version>
        <maven.resources.plugin.version>3.3.1</maven.resources.plugin.version>
        
        <!-- Test Configuration -->
        <karate.env>dev</karate.env>
        <karate.options></karate.options>
        <test.parallel.threads>5</test.parallel.threads>
    </properties>

    <dependencies>
        <!-- Karate JUnit 5 Integration -->
        <dependency>
            <groupId>com.intuit.karate</groupId>
            <artifactId>karate-junit5</artifactId>
            <version>${karate.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Karate Core -->
        <dependency>
            <groupId>com.intuit.karate</groupId>
            <artifactId>karate-core</artifactId>
            <version>${karate.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- JUnit 5 API -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit5.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- JUnit 5 Engine -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit5.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- JUnit 5 Params for Parameterized Tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-params</artifactId>
            <version>${junit5.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Gherkin Parser -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>gherkin</artifactId>
            <version>${gherkin.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Cucumber Java -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Cucumber JUnit Platform -->
        <dependency>
            <groupId>io.cucumber</groupId>
            <artifactId>cucumber-junit-platform-engine</artifactId>
            <version>${cucumber.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- SLF4J Logging API -->
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <version>${slf4j.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Logback Classic -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
            <scope>test</scope>
        </dependency>
        
        <!-- JSON Processing -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>2.15.3</version>
            <scope>test</scope>
        </dependency>
        
        <!-- Apache Commons for Utilities -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
            <version>3.13.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- Include feature files from java source directory -->
        <testResources>
            <testResource>
                <directory>src/test/java</directory>
                <excludes>
                    <exclude>**/*.java</exclude>
                </excludes>
            </testResource>
            <testResource>
                <directory>src/test/resources</directory>
            </testResource>
        </testResources>

        <plugins>
            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.compiler.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Maven Resources Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>${maven.resources.plugin.version}</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>

            <!-- Maven Surefire Plugin for Unit Tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${maven.surefire.plugin.version}</version>
                <configuration>
                    <argLine>-Dfile.encoding=UTF-8</argLine>
                    <includes>
                        <include>**/*Runner.java</include>
                        <include>**/*Test.java</include>
                        <include>**/Test*.java</include>
                    </includes>
                    <excludes>
                        <exclude>**/*IT.java</exclude>
                    </excludes>
                    <systemPropertyVariables>
                        <karate.env>${karate.env}</karate.env>
                        <karate.options>${karate.options}</karate.options>
                    </systemPropertyVariables>
                    <testFailureIgnore>false</testFailureIgnore>
                    <reuseForks>true</reuseForks>
                    <forkCount>1</forkCount>
                </configuration>
            </plugin>

            <!-- Maven Failsafe Plugin for Integration Tests -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>${maven.failsafe.plugin.version}</version>
                <configuration>
                    <includes>
                        <include>**/*IT.java</include>
                        <include>**/*IntegrationTest.java</include>
                    </includes>
                    <systemPropertyVariables>
                        <karate.env>${karate.env}</karate.env>
                    </systemPropertyVariables>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>

    <!-- Test Execution Profiles -->
    <profiles>
        <!-- Default Profile - Run All Tests -->
        <profile>
            <id>default</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>

        <!-- Smoke Tests Profile -->
        <profile>
            <id>smoke</id>
            <properties>
                <karate.options>--tags &#64;smoke</karate.options>
            </properties>
        </profile>

        <!-- Critical Tests Profile -->
        <profile>
            <id>critical</id>
            <properties>
                <karate.options>--tags &#64;critical</karate.options>
            </properties>
        </profile>

        <!-- High Priority Tests Profile -->
        <profile>
            <id>high</id>
            <properties>
                <karate.options>--tags &#64;high</karate.options>
            </properties>
        </profile>

        <!-- Security Tests Profile -->
        <profile>
            <id>security</id>
            <properties>
                <karate.options>--tags &#64;security</karate.options>
            </properties>
        </profile>

        <!-- Performance Tests Profile -->
        <profile>
            <id>performance</id>
            <properties>
                <karate.options>--tags &#64;performance</karate.options>
            </properties>
        </profile>

        <!-- Integration Tests Profile -->
        <profile>
            <id>integration</id>
            <properties>
                <karate.options>--tags &#64;integration</karate.options>
            </properties>
        </profile>

        <!-- Contract Tests Profile -->
        <profile>
            <id>contract</id>
            <properties>
                <karate.options>--tags &#64;contract</karate.options>
            </properties>
        </profile>

        <!-- Pet API Tests Profile -->
        <profile>
            <id>pet</id>
            <properties>
                <karate.options>--tags &#64;pet</karate.options>
            </properties>
        </profile>

        <!-- Store API Tests Profile -->
        <profile>
            <id>store</id>
            <properties>
                <karate.options>--tags &#64;store</karate.options>
            </properties>
        </profile>

        <!-- User API Tests Profile -->
        <profile>
            <id>user</id>
            <properties>
                <karate.options>--tags &#64;user</karate.options>
            </properties>
        </profile>

        <!-- Positive Tests Profile -->
        <profile>
            <id>positive</id>
            <properties>
                <karate.options>--tags &#64;positive</karate.options>
            </properties>
        </profile>

        <!-- Negative Tests Profile -->
        <profile>
            <id>negative</id>
            <properties>
                <karate.options>--tags &#64;negative</karate.options>
            </properties>
        </profile>

        <!-- CI/CD Pipeline Profile -->
        <profile>
            <id>ci</id>
            <properties>
                <karate.options>--tags &#64;smoke,&#64;critical</karate.options>
                <test.parallel.threads>10</test.parallel.threads>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <parallel>methods</parallel>
                            <threadCount>${test.parallel.threads}</threadCount>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Parallel Execution Profile -->
        <profile>
            <id>parallel</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <parallel>methods</parallel>
                            <threadCount>${test.parallel.threads}</threadCount>
                            <perCoreThreadCount>true</perCoreThreadCount>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!-- Development Environment Profile -->
        <profile>
            <id>dev</id>
            <properties>
                <karate.env>dev</karate.env>
            </properties>
        </profile>

        <!-- Staging Environment Profile -->
        <profile>
            <id>staging</id>
            <properties>
                <karate.env>staging</karate.env>
            </properties>
        </profile>

        <!-- Production Environment Profile -->
        <profile>
            <id>prod</id>
            <properties>
                <karate.env>prod</karate.env>
            </properties>
        </profile>
    </profiles>

</project>
